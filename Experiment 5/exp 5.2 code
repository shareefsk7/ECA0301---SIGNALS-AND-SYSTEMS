clc;
clear;
clf;

N = 20;
n = 0:N-1;

// Unit step input
x = ones(1,N);

// Output
y = zeros(1,N);

for k = 2:N
    y(k) = 0.5*y(k-1) + x(k);
end
y(1) = x(1);

subplot(2,1,1);
plot2d3(n,x);
title("Unit Step Input");

subplot(2,1,2);
plot2d3(n,y);
title("Step Response");
