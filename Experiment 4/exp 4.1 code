clc;
clear;
clf;

N = 20;
n = 0:N-1;

x = ones(1,N);
y = zeros(1,N);

for k = 2:N
    y(k) = 0.5*y(k-1) + x(k);
end

subplot(2,1,1);
plot2d3(n,x);
title("Input Sequence");

subplot(2,1,2);
plot2d3(n,y);
title("Output Sequence");
